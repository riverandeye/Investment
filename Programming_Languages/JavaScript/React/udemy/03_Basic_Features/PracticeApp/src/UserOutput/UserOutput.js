import React, {useState} from 'react';

const UserOutput = (props)=>{
    
    return (
        <div>
            <p1>{props.username}</p1>
            <p2></p2>
        </div>
    );
}

export default UserOutput;